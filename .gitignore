# Scratch for the superpowers SDD workflow; never committed.
.superpowers/

node_modules/
dist/
# `deno task package:desktop` writes the unpacked application here (Electron's runtime, ~250 MB).
out/
.angular/
coverage/
*.tsbuildinfo
.spm/
test-tmp/
# Playwright's own output directories.
test-results/
playwright-report/

*.env